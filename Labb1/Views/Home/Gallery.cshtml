@model List<Labb1.Models.ModelGallery>
@{
    ViewBag.Title = "My Photo Gallery";

}
<h2>@ViewBag.Title.</h2>
<h3>@ViewBag.Message</h3>
@{ 
 foreach (var image in Model)
 {
        <img src="~/Images/@image.Name" width="400px" />

    <div class="caption">
        <h2>@image.Id</h2>
        <h2>@image.Name</h2>
        @Html.ActionLink("Show Image", "ShowImage", "Home", new { Id = image.Id }, null)
        @Html.ActionLink("Delete Image", "DeleteAImage", "Home", new { Id = image.Id }, null)
    </div>
 }
}

@section bottom{ 
<h2> @Html.Label("Latest image uploaded"):</h2>
    <img src="~/Images/@Model.Last().Name" width="100px" />
    <p>@Html.Label(Model.Last().Name.ToString())</p>
    <p>@Html.Label(Model.Last().Id.ToString())</p>
}
